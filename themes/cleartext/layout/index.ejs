<div class="posts-grid">
<% page.posts.each(function(post) { %>
<article class="post-card" itemscope itemtype="http://schema.org/Article">
  <h2 class="post-card-title" itemprop="headline">
    <a href="<%= url_for(post.path) %>" itemprop="url"><%= post.title %></a>
  </h2>

  <div class="post-card-meta">
    <span class="post-card-date" itemprop="datePublished" content="<%= date_xml(post.date) %>">
      <time datetime="<%= date_xml(post.date) %>">
        <svg class="post-card-date-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        <%= full_date(post.date, theme.article.date_format || 'YYYY-MM-DD') %>
      </time>
    </span>
    <% if (post.updated && post.updated !== post.date) { %>
    <span class="post-card-updated">更新于 <%= full_date(post.updated, theme.article.date_format || 'YYYY-MM-DD') %></span>
    <% } %>
  </div>

  <% if (post.excerpt) { %>
  <div class="post-card-excerpt">
    <%= strip_html(post.excerpt) %>
  </div>
  <% } %>

  <% if (post.categories && post.categories.length > 0 || post.tags && post.tags.length > 0) { %>
  <div class="post-card-meta-bottom">
    <% if (post.categories && post.categories.length > 0) { %>
    <% post.categories.each(function(cat) { %>
    <a href="<%= url_for(cat.path) %>" class="category-badge"><%= cat.name %></a>
    <% }); %>
    <% } %>
    <% if (post.tags && post.tags.length > 0) { %>
    <% post.tags.each(function(tag) { %>
    <a href="<%= url_for(tag.path) %>" class="tag-badge"><%= tag.name %></a>
    <% }); %>
    <% } %>
  </div>
  <% } %>
</article>
<% }); %>
</div>

<!-- Pagination -->
<% if (page.total > 1) { %>
<%- partial('partial/pagination') %>
<% } %>
