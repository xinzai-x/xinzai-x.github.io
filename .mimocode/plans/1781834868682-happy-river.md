# 修复 blockquote 字体 + 代码高亮

## 1. blockquote 字体减小
`style.css:892` 添加 `font-size: 0.95rem`

## 2. 代码高亮
CSS 规则已在 highlight.css 中（`.keyword`, `.string`, `.comment` 等），HTML class 也正确。最可能是浏览器缓存。用户需 Ctrl+F5 硬刷新。

如果硬刷新后仍无高亮，可以在 head.ejs 的 CSS URL 后加版本号强制刷新缓存：`css('css/highlight.css?v=2')`。

## 文件
- `themes/ink/source/css/style.css:892` — blockquote 加 font-size
- `themes/ink/layout/_partial/head.ejs:32` — 可选：CSS URL 加版本号

## 部署
```
hexo cl; hexo g; hexo d
```
